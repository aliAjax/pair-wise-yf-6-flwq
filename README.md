# 家庭维修事项

端口规划：`5114`

```bash
npm install
npm run dev
```

第一版闭环：记录维修问题、按状态查看、更新处理状态、统计未完成预计费用，支持照片链接并使用localStorage保存数据。

第二版：备份与恢复。导出 JSON 备份文件后可到另一台设备导入；导入前整份校验，不合格则当前数据原样保留；两边按事项编号合并去重，同编号取最后修改的一份，已删除事项靠移除标记挡住旧备份；导入完成列出新增、更新、移除、跳过数量；旧记录缺编号或修改时间会自动补齐。
