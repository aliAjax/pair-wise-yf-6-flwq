# 家庭维修事项

端口规划：`5114`

```bash
npm install
npm run dev
```

第一版闭环：记录维修问题、按状态查看、更新处理状态、统计未完成预计费用，支持照片链接并使用localStorage保存数据。

备份与接续：导出 JSON 备份拷到另一台设备导入；导入前整份校验（缺字段或格式不对整体拒绝，当前数据不变），按事项编号合并去重、同一编号取最后修改的一份，已删除事项靠移除标记挡住旧备份。导入完成显示新增、更新、移除、跳过数量；旧记录缺编号或修改时间会自动补齐。
